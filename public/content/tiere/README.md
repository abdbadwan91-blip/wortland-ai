# Tiere assets

Picture Match Level 1 uses large emoji embedded in Learning Objects
(`src/modules/Content/tiere.ts`). Optional illustrated SVGs can be dropped
here later and referenced via `imageKind: 'url'`.
